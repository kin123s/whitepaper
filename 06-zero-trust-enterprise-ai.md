# Zero-Trust 기반 Enterprise AI: 폐쇄망 환경에서의 안전한 에이전트 오케스트레이션

> **Author:** ycy0922  
> **Version:** 1.0  
> **Date:** 2026-04-03  
> **Status:** Portfolio Document

---

## Abstract

현재 AI 코딩 어시스턴트(Copilot, Cursor 등)는 개발자의 생산성을 획기적으로 끌어올리지만, 엔터프라이즈 사내망(폐쇄망) 환경에 도입될 때는 치명적인 보안 리스크를 안고 있다. AI가 통제망을 벗어나 사내 DB를 파괴적으로 조작하거나, 소스 코드 및 민감 데이터를 외부 퍼블릭 LLM 서버로 유출할 위험이 뒤따르기 때문이다.

본 문서는 사내망 운영 환경과 보안 아키텍처에 대한 관리자(Genians 인프라/그룹웨어 담당)로서의 전문성을 바탕으로, **"아무것도 신뢰하지 않는다(Zero-Trust)"**는 원칙을 AI 에이전트 실행 환경에 녹여낸 아키텍처를 소개한다. 

네트워크, 컨테이너, 그리고 AI 모델 수준에서 각각의 통제 구역을 설정하여, "오케스트레이션의 유연성"과 "엔터프라이즈 수준의 데이터 보호"라는 두 마리 토끼를 잡은 설계 전략을 설명한다.

---

## Table of Contents

1. [Enterprise AI 도입의 현실적 리스크 (Threat Model)](#1-enterprise-ai-도입의-현실적-리스크)
2. [아키텍처 원칙: AI 샌드박스와 통제망 분리](#2-아키텍처-원칙-ai-샌드박스와-통제망-분리)
3. [Container 레벨 보안: Docker 기반 격리 및 I/O 권한 제어](#3-container-레벨-보안-docker-기반-격리-및-io-권한-제어)
4. [Data & Network 레벨 보안: Local-First 프록싱](#4-data--network-레벨-보안-local-first-프록싱)
5. [Resource 레벨 보안: 워크로드 보호를 위한 동적 트로틀링](#5-resource-레벨-보안-워크로드-보호를-위한-동적-트로틀링)
6. [Identity 레벨 보안: 텍스트 기반 도메인 격리](#6-identity-레벨-보안-텍스트-기반-도메인-격리)
7. [결론 및 향후 과제](#7-결론-및-향후-과제)

---

## 1. Enterprise AI 도입의 현실적 리스크

기업 인프라 운영자 관점에서, 외부 AI 에이전트에게 내부 시스템의 제어권을 부여할 때 발생하는 주요 위협 모델(Threat Model)은 다음과 같다:

1. **Destructive Actions (파괴적 행동):** 환각(Hallucination)에 빠진 AI가 사내망 MAIN DB에 연결해 권한 없는 `DELETE`나 대규모 `UPDATE` 스크립트를 생성하고 실행할 위험.
2. **Data Exfiltration (데이터 유출):** 외부 LLM(API) 공급자 측으로 사내 주요 환경 변수(.env), 라이선스 키, 인프라의 절대 경로 정보가 프롬프트 전송 과정에서 유출될 위험.
3. **Cross-Project Leak (프로젝트 간 교차 오염):** A 프로젝트(대외비) 작업을 위해 로출된 컨텍스트가 B 프로젝트(외부 공개) 작업 중에 활용되어 영업 비밀이 섞여 들어갈 위험.
4. **Denial of Service (자원 소진):** RAG 인덱싱 과정이나 과도한 AI 탐색 로직이 호스트 장비의 CPU, 메모리 자원을 100% 점유하게 만들어 실제 배포된 내부 서비스가 장애를 겪는 현상.

---

## 2. 아키텍처 원칙: AI 샌드박스와 통제망 분리

위 문제들을 해결하기 위해 전체 시스템을 철저히 샌드박스화(Sandboxing)된 구역으로 분리했다.

```text
┌─────────────────┐       ┌─────────────────┐       ┌─────────────────┐
│ [ Zone 1 ]      │       │ [ Zone 2 ]      │       │ [ Zone 3 ]      │
│ IDE Client      │       │ MCP Middleware  │       │ Secure Backend  │
│ (VS Code Ext)   │ ────▶ │ (Node.js/Docker)│ ────▶ │ (L_DB / Legacy) │
│ • 권한 부여      │  망  │ • Read-Only 볼륨 │  방  │ • 원천 데이터 보호│
│ • 명령어 허가    │  분  │ • 로컬 LLM 통제  │  화  │ • 접근 제어(SSO) │
│                 │  리  │                 │  벽  │                 │
└─────────────────┘       └─────────────────┘       └─────────────────┘
```

AI 에이전트(Zone 1)는 결코 Zone 3에 직접 손을 댈 수 없으며, 반드시 미들웨어(Zone 2)의 한정된 도구들을 통해서만 정보를 요청할 수 있게 설계되었다.

---

## 3. Container 레벨 보안: Docker 기반 격리 및 I/O 권한 제어

가장 물리적이고 직관적인 방어선은 컨테이너 레벨의 격리 구조다. 

- **Read-Only (ro) 볼륨 마운트:**  
  AI에게 제공할 지식 저장소(Knowledge Base), 소스 코드 디렉토리 등은 모두 Docker Compose에서 `:ro` 플래그를 붙여 호스트 볼륨에 마운트했다.
  ```yaml
  volumes:
    - ${KNOWLEDGE_DIR}:/data/company:ro  # 절대로 컨테이너 안에서 수정할 수 없음
  ```
  이로 인해 악의적이거나 극단적으로 오류가 난 프롬프트가 MCP 도구를 통해 원본 데이터를 파괴하는 것을 원천(OS 레벨) 차단한다.
- **Node_modules 익명 볼륨 적용 최적화:**  
  개발망 환경 구성 시 Windows 호스트 I/O 병목을 해결함과 동시에, 컨테이너 내부의 환경오염을 로컬로 전파시키지 않기 위해 익명 볼륨 맵핑 기법을 활용하여 컨테이너 환경의 순수성을 보장했다.

---

## 4. Data & Network 레벨 보안: Local-First 프록싱

외부로 전송되는 쿼리에 민감한 데이터가 포함되는 것을 막기 위한 데이터 파이프라인 보안 설계다.

- **사내 구축형 Ollama 배치:**  
  임베딩(Vector 변환)이나 1차 문서 요약처럼 "데이터 자체를 읽어 이해해야 하는" 작업은 외부망(API)을 타지 않고 반드시 사내망에 배치된(Local-first) 로컬 모델(llama3:8b 등)을 사용하여 네트워크 밖으로 반출되는 문서 자체를 제로(Zero)화 했다.
- **RESTrictive MCP Tools:**  
  제공된 `14개의 MCP Tools` 목록을 보면 `direct_sql_query()` 나 `execute_shell()` 같은 치명적 함수가 아예 존재하지 않는다. AI는 샌드박스가 허용한 `search_memory`, `query_context` 같은 읽기 전용 검색 도구만을 호출하도록 설계했다. 변경(Write)은 오직 세션 상태 갱신 같은 제한된 JSON 구조에만 허용된다.

---

## 5. Resource 레벨 보안: 워크로드 보호를 위한 동적 트로틀링

Zero-Trust는 외부 방어뿐 아니라 '내부 리소스에 대한 제어'까지 포함한다. 백엔드 AI 에이전트(주로 GraphRAG 인덱싱 데몬)가 사내 운영 장비나 데스크톱 환경의 리소스를 폭주시키지 못하도록 보호 장치를 두었다.

- **SystemProbe 기반 리소스 제어:**  
  호스트 운영체제의 CPU 사용률, 물리 메모리 반환율, Node.js의 EventLoop 레이턴시를 밀리초(ms) 단위로 측정하는 프로브(Probe)를 장착했다. 
- **Resource-Aware Scheduling:**  
  모니터링 결과가 부하 상태(Busy)를 가리키면, 백그라운드 인덱싱 작업의 Thread 배분을 줄이거나 의도된 Sleep(5,000ms 등)을 강제 주입해 로드 밸런싱을 맞췄다. (메인 개발 런타임의 쾌적함을 최우선 보장)

---

## 6. Identity 레벨 보안: 텍스트 기반 도메인 격리

모노레포와 다중 프로젝트를 수용하기 위해, 각 프로젝트의 성격이 섞이지 않도록(Cross-Project Leak) 상태를 격리했다.

- **SSOT 방식의 L1/L2 시스템:** (참고: [Chapter 1](./01-ai-agent-governance-architecture.md))  
  어느 프로젝트를 열든 최상위 루트 중앙 보안 허브(L1)에서 승인된 보안 규칙 문서(`guardrails.md`)가 최우선으로 투입되며, 특정 프로젝트만 열람 가능한 L2 분리 체계를 강제하여 권한 밖의 지식에 AI가 접근하는 것을 원천 방지했다.
- **물리적 Session ID 분리 분배:**  
  실행 워크플로우에 따라 파일 시스템에 쿼리 세션의 범주가 물리적 디렉토리 단위로 나뉘어 있어, A라는 프로젝트 폴더의 이슈 세션이 B라는 폴더 구역의 데이터를 임의 병합(Merge)하지 못하게 방벽을 세웠다.

---

## 7. 결론 및 향후 과제

Zero-Trust 방식의 도입은 AI의 "자율성 무한 확장"을 막는 것이 아니라, "안전한 경계 안에서의 극대화된 자율성 보장"을 의미한다. 네트워크 분리, 도커 레벨 권한 통제, 로컬 LLM 적극 활용 등을 통해 "기업에서도 당장 AI 오케스트레이션을 꺼릴 이유가 없음"을 기술적으로 입증했다.

**[Next Step]** 현재 기획 중인 `prj_jira_extension` (OmniSync)과 자체 개발 라이선스 서버(Jira License Server) 연동이 궤도에 오르면, 본 아키텍처는 단순한 로컬 샌드박스를 넘어, 엔터프라이즈 환경 전역의 라이선스 체크 및 접근 인가까지 통제 가능한 완성형 폐쇄망 클라우드 네이티브 아키텍처로 발돋움할 수 있을 것이다. 

---

*© 2026 ycy0922. 본 문서는 개인 포트폴리오 목적으로 작성되었으며, 사내 기밀 정보는 포함되어 있지 않습니다.*
