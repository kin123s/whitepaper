# AI 엔진의 진화: Vector DB의 한계를 넘어 GraphRAG와 자가 교정 시스템으로

> **Author:** ycy0922  
> **Version:** 1.0  
> **Date:** 2026-04-03  
> **Status:** Portfolio Document  
> **Companion to:** [Chapter 4 — MCP RAG Server Architecture](./04-mcp-rag-server-architecture.md)

---

## Abstract

[Chapter 4](./04-mcp-rag-server-architecture.md)에서 다루었던 초기 RAG 서버(ChromaDB + TF-IDF 폴백) 구조는 AI가 "추측하지 않고 검색"하게 만드는 데 성공적이었다. 그러나 사내망 깊숙이 자리 잡은 레거시 코드와 수많은 릴리스 기록이 얽힌 엔터프라이즈 환경에서, 단순 벡터 기반 검색은 "다단계 구조(Multi-hop)"를 파악하지 못해 치명적인 오답을 낳았다.

본 문서는 초기 RAG 아키텍처의 한계를 수치적으로 진단하고, 이를 타개하기 위해 **Neo4j 기반 Knowledge Graph (GraphRAG)**를 도입한 과정을 기록한다. 더불어 BM25 + Embedding + Graph Traversal을 융합한 **Triple-Hybrid Search**와, LLM 스스로 검색 결과를 평가하는 **Self-RAG (자가 교정 루프)** 아키텍처를 도입하여 토큰 낭비를 줄이고 검색 정확도를 극대화한 아키텍트 관점의 트러블슈팅 스토리다.

---

## Table of Contents

1. [문제 제기: Vector RAG의 3가지 근본적 한계](#1-문제-제기-vector-rag의-3가지-근본적-한계)
2. [아키텍처 레이아웃: v11.0 통합 구조](#2-아키텍처-레이아웃-v110-통합-구조)
3. [GraphRAG 도입: 단어 거리가 아닌 트랜잭션 거리로](#3-graphrag-도입-단어-거리가-아닌-트랜잭션-거리로)
4. [Triple-Hybrid Search 파이프라인](#4-triple-hybrid-search-파이프라인)
5. [Self-RAG 루프: 쓰레기가 들어가면 다시 찾게 하라](#5-self-rag-루프-쓰레기가-들어가면-다시-찾게-하라)
6. [의도적 리소스 통제: Resource-Aware Scheduling](#6-의도적-리소스-통제-resource-aware-scheduling)
7. [성과 지표(KPI) 및 교훈](#7-성과-지표kpi-및-교훈)

---

## 1. 문제 제기: Vector RAG의 3가지 근본적 한계

운영 6개월 차에 접어들며, 초기 아키텍처(v6.0~v9.0)가 다음과 같은 치명적인 한계에 봉착했다.

| 한계점 | 현상 및 원인 | 타격 수준 |
|--------|--------------|-----------|
| **Multi-hop 질문 실패** | "A 모듈을 수정한 커밋에 연결된 Jira 이슈 코드는?" 같은 다단계 질문에 답하지 못함. 벡터 DB는 단일 쿼리 매칭만 가능하기 때문. | 🔴 Critical |
| **고유명사 및 약어 소실** | `DOMAIN_EX`, `M_API` 같은 조직 내부의 규칙 없는 약어들이 영어 기반 임베딩(MiniLM) 차원에서 의미가 희석되어 엉뚱한 테이블을 매칭함. | 🔴 Critical |
| **GIGO (환각 증폭)** | 잘못된 검색 결과가 Context로 넘어가면, AI 에이전트는 이를 사실로 믿고 돌이킬 수 없는 소설(Hallucination)을 작성한 뒤 확정해버림. | 🟡 High |

결국 **"더 좋은 임베딩 모델"**로 교체하는 1차원적 접근을 버리고, **"검색 전략과 평가 시스템 자체"**를 갈아엎어야 했다. 이를 통해 ChromaDB를 걷어내고 현재의 v11.0 구조로 진화하게 된다.

---

## 2. 아키텍처 레이아웃: v11.0 통합 구조

무거웠던 Python Sidecar를 덜어내고, 확장 가능하고 독립적인 노드 런타임 환경(+Neo4j+Ollama)으로 재편했다.

```text
┌─────────────────────────────────────────────────────────┐
│  AI IDE (Cursor / Gemini CLI / Claude)                  │
│    ↕ stdio (Model Context Protocol)                     │
├─────────────────────────────────────────────────────────┤
│  mcp-rag-server v11.0 (TypeScript, Node.js 20+)        │
│                                                         │
│  ┌──────▼──────────────────▼──────────────────────────┐ │
│  │         Triple-Hybrid Search Engine                │ │
│  │  ┌──────────┐    ┌────────────────┐                │ │
│  │  │ BM25     │    │ Embedding      │                │ │
│  │  │ (FTS5)   │───▶│ Rerank (Top5)  │                │ │
│  │  │ Top 50   │    └───────┬────────┘                │ │
│  │  └──────────┘            │                         │ │
│  │  ┌──────────────────────────────────┐              │ │
│  │  │ ★ Neo4j Graph Traversal         │              │ │
│  │  │   Entity Linking → N-hop Search  │              │ │
│  │  └──────────┬───────────────────────┘              │ │
│  │             ▼                                      │ │
│  │  ┌──────────────────────────────────┐              │ │
│  │  │ Self-RAG: Relevance Judge        │              │ │
│  │  │   ≥40% → 통과    <40% → Rewrite │ ──(재시도)───┘ │
│  │  └──────────────────────────────────┘              │ │
│  └─────────────┼──────────────────────────────────────┘ │
│                ▼                                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Context      │  │ KPI Metrics  │  │ Stealth      │  │
│  │ Compression  │  │ Collector    │  │ Queue Worker │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
         ↕ HTTP                    ↕ Bolt
    ┌──────────────────┐    ┌──────────────────┐
    │ Ollama (Local)   │    │ Neo4j 5          │
    │ • nomic-embed    │    │ (Community)       │
    │ • llama3:8b      │    │ Knowledge Graph   │
    └──────────────────┘    └──────────────────┘
```

---

## 3. GraphRAG 도입: 단어 거리가 아닌 트랜잭션 거리로

Multi-hop 질문의 실패 원인은 데이터가 평면적으로 분리되어 있었기 때문이다. v11.0에서는 코드의 함수, 파일, Git 커밋 해시, Jira 이슈 티켓을 각기 다른 **노드(Node)**로 지정하고, 이들 사이의 의존성(수정, 호출, 매핑)을 **엣지(Edge)**로 구축하는 Neo4j 지식 그래프를 구성했다.

### 기존 Vector vs Graph Traversal 비교

- **기존 (Vector Only):** "결제 모듈 에러" 검색 ➔ 결제 모듈 코드가 담긴 마크다운 파일만 반환. (해당 모듈을 수정한 배포 내역 확인 불가)
- **개선 (GraphRAG):** "결제 모듈 에러" 검색 ➔ 모듈 파일(Node) 발견 ➔ 해당 파일을 가장 최근 변경한 커밋(Node) 탐색 ➔ 그 커밋과 연동된 Jira 에픽 티켓(Node) 반환 ➔ 전체 맥락을 한 번에 제공.

지식의 거리를 '단어들의 유사도'가 아니라, '비즈니스 로직의 트랜잭션 깊이(Depth)'로 추적할 수 있게 된 것이 핵심이다.

---

## 4. Triple-Hybrid Search 파이프라인

단일 검색 방식의 맹점을 없애고 응답 시간을 단축하기 위해 검색을 3단계로 분리했다.

| 단계 | 엔진 파이프라인 | 목적 및 장점 | 속도 |
|------|---------------|--------------|------|
| **1. BM25** | SQLite FTS5 | 하드 키워드 속도전. `DOMAIN_EX`, `PDO` 같은 텍스트 정확도 보장 및 검색 후보 50개 추리기. | < 10ms |
| **2. Embedding** | Ollama (nomic-embed) | 50개의 후보군을 문맥의 유사도에 따라 재배열(Reranking)하여 상위 5개를 추출. | < 200ms |
| **3. Graph** | Neo4j N-hop Traversal | 추출된 Top 5 문서(Entity)를 기점으로 Neo4j 내에서 관련된 모듈, 변경 이력 등을 N-hop 거쳐 순회, 맥락 보존 정보 보충. | ~ 800ms |

이 **Triple-Hybrid 패턴**은 고유명사의 소실을 막는 동시에(BM25 파워), 자연어 문맥을 채치하고(Embedding 파워), 파편화된 비즈니스의 인과관계를 찾아내는(GraphRAG 파워) 가장 완벽한 조합이 되었다.

---

## 5. Self-RAG 루프: 쓰레기가 들어가면 다시 찾게 하라

검색 결과가 부실할 때 발생하는 AI의 엉뚱한 결론(할루시네이션)을 방어하기 위해 도입된, LLM 스스로 검색 결과를 평가하는 로직이다.

1. **Relevance Judge (관련도 판사):**  
   검색이 완료된 Context 문서를 로컬 모델(llama3:8b)에 던져, "이 문서가 에이전트의 원래 질문에 대답하기 충분한가?"를 0.0 ~ 1.0 점수 체계로 평가받는다. 
   - **Threshold (40%) 이상:** 품질 승인 ➔ 본 에이전트에게 데이터 반환.
2. **Query Rewriter (쿼리 재작성기):**  
   - **Threshold 미만:** 품질 불합격 ➔ 쿼리 재작성 모듈 호출.  
   단순 실패를 던지는 대신 원본 질문을 확장, 분해("결제 에러" ➔ "결제 API Exception", "PG사 통신 타임아웃 DB 에러" 등)하여 검색 파이프라인 처음으로 돌아가 재탐색을 2회까지 수행한다.

이 체계 확립 후, 오답을 고집하며 무한 루프를 돌던 에이전트의 토큰 소모율을 획기적으로 낮출 수 있었다.

---

## 6. 의도적 리소스 통제: Resource-Aware Scheduling

이러한 고도화된 색인 및 그래프 매핑 작업은 사내 호스트 시스템의 커다란 CPU/Mem 스파이크를 유발할 수 있다. 인프라 운영 관리 관점에서, 메인 서비스나 업무 시스템에 병목을 주어선 안 된다.

그래서 도입한 것이 **SystemProbe 기반의 스텔스(Stealth) 큐 워커**다.
- OS의 실시간 이벤트 루프 지연도 및 가용 메모리를 스캔.
- 상태가 `Busy`일 경우, 인덱싱과 임베딩 작업을 5초(5000ms) 이상 지연(Throttle)시키며 배치 워크로드를 분산.
- 이 설계는 백엔드의 AI 미들웨어가 눈에 띄지 않게(스텔스) 그림자처럼 동작하도록 보장하는 최고 수준의 방어 기재가 되었다.

---

## 7. 성과 지표(KPI) 및 교훈

기존 Vector 단일 검색 시절(v9.0 이전)과 GraphRAG + SelfRAG 체계(v11.0)를 대조한 자동 수집 KPI 데이터 비교다.

| 측정 지표 | Before (Vector RAG) | After (Graph + Self-RAG) | 결과 |
|-----------|--------------------|--------------------------|------|
| **Multi-hop 질문 답변율** | ~20% 이하 성공 | **~85% 이상 성공** | 압도적 향상 |
| **사내 고유명사 적중률** | ~40% (임베딩 희석) | **99% (BM25 필터 개입)** | 정확도 극대화 |
| **Hallucination 빈도** | 5회 호출 당 1회 오답 | **20회 당 1회 미만** | 신뢰성 및 오류 방어 |
| **컨텍스트 토큰 소모율** | 통짜 파일 주입 | **40% 수준으로 요약/압축** | 운영 비용 최적화(Cost ↓) |

### 아키텍트의 교훈

1. **최신 모델보다 아키텍처 파이프라인이 정답이다:**  
   수백 기가의 최신 LLM 서버를 구동할 필요 없이, 가벼운 로컬 모델이어도 검색 체계가 단계별로 논리적이면 훨씬 똑똑하고 민첩하게 동작한다.
2. **에러를 일찍 인정하라(Fail-Fast):**  
   Self-RAG 루프는 "이상하면 빨리 던지고 재시도한다"는 원칙의 실현이다. 잘못된 데이터로 장시간 추론하는 것을 조기에 차단하는 것이 전체 시스템 안정성 확보의 핵심 열쇠였다.

본 진화 과정은 단순한 기술 트렌드의 맹목적 추종이 아닌, 실측된 KPI를 바탕으로 "왜" 진화해야 했는지 증명한 10년 차 아키텍트의 문제 해결 과정이기도 하다.

---

*© 2026 ycy0922. 본 문서는 개인 포트폴리오 목적으로 작성되었으며, 사내 기밀 정보는 포함되어 있지 않습니다.*
